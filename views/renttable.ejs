<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Renting</title>
    <style>
      .header {
            min-height: 100vh; 
            width: 100%;
            background-image: url(https://png.pngtree.com/thumb_back/fh260/background/20210507/pngtree-purple-blue-crystal-image_708512.jpg);
            background-position: center;
            display: flex; 
            color: white; 
            background-size: cover;
            background-position: center;}
            .text-center{padding-top: 80px;}
      .tablecolor{background-color: lightcoral; padding:10px; text-align: center; margin:auto;}
      th,
        td {
            border: 3px solid #dddddd;
            padding: 15px;
            width:5%;
            text-align: left;
            font-size: 1em;
            border-style:dotted;
        }
      th{border: 5px solid white; border-style: dashed;}
      .addWorkout-container {
            position: fixed;
            bottom: 100px;
            left: 50%;
            transform: translateX(-50%);
        }
      .addWorkout {
            text-align: center;
            font-size: 1.2em;
            text-decoration: none;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border-radius: 5px;}
    </style>
</head>
<body class="middle">
  <section class="header">
    <%- include ('partials/navbar.ejs') %>
    <div class="container">
        <br>
        <div class="text-center"><h2>Rent Details</h2></div>
        <br>
        <table class="tablecolor" >
          <thead>
              <tr>
                <th class="black" width="50">Name</th>
                <th width="100">Location</th>
                <th width="50">Date</th>
                <th width="50">Edit</th>
                <th width="50">Delete</th>
              </tr>
          </thead>
          <tbody>
            <% for(let i=0; i < rent.length; i++) { %>
              <tr>
                <td><%= rent[i].Name %></a></td>
                <td><%= rent[i].Location %></td>
                <td><%= rent[i].Date.toLocaleDateString() %></td>
                <td><a href="/editrent/<%= rent[i].RentID %>">Edit</a></td>
                <td><a href="/deleterent/<%= rent[i].RentID %>" onclick="return confirm('Are you sure you want to delete this record?')">Delete</a></td>
              </tr>
            <% } %>
          </tbody>
        </table>
    </div>
    <div class="addWorkout-container">
      <a class="addWorkout" href="/addrent">Add Rent</a>
    </div>
  </section>
</body>
</html>
